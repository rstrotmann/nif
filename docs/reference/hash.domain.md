# Generate the XXH128 hash of a SDTM domain object

Generate the XXH128 hash of a SDTM domain object

## Usage

``` r
# S3 method for class 'domain'
hash(obj)
```

## Arguments

- obj:

  A SDTM domain object.

## Value

The XXH128 hash of the nif object as character.
