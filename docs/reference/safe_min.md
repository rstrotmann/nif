# Min derivative that works with vectors containing NaN

Min derivative that works with vectors containing NaN

## Usage

``` r
safe_min(x, ...)
```

## Arguments

- x:

  The input as numeric.

## Value

The mean as numeric.
