# SD derivative that works with vectors containing NaN

SD derivative that works with vectors containing NaN

## Usage

``` r
safe_sd(x)
```

## Arguments

- x:

  The input as numeric.

## Value

The mean as numeric.
