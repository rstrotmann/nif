# Mean derivative that works with vectors containing NaN

Mean derivative that works with vectors containing NaN

## Usage

``` r
safe_mean(x, ...)
```

## Arguments

- x:

  The input as numeric.

## Value

The mean as numeric.
