# Synthetic NIF data from a fictional clinical single-arm study with rich and sparse pk sampling. Reduced to the minimally required fields.

Synthetic NIF data from a fictional clinical single-arm study with rich
and sparse pk sampling. Reduced to the minimally required fields.

## Usage

``` r
examplinib_poc_min_nif
```

## Format

A NIF object
