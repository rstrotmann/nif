# The list of expected date/time formats as per ISO 8601

The list of expected date/time formats as per ISO 8601

## Usage

``` r
dtc_formats
```

## Format

An object of class `character` of length 7.
