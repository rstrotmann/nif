# Generate the XXH128 hash of a nif object

Generate the XXH128 hash of a nif object

## Usage

``` r
hash.nif(obj)
```

## Arguments

- obj:

  A nif object.

## Value

The XXH128 hash of the nif object as character.
