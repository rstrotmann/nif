% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nif_observations.R
\name{make_ntime_from_tpt}
\alias{make_ntime_from_tpt}
\title{Convert time point text to numeric time values}
\usage{
make_ntime_from_tpt(obj, domain = NULL)
}
\arguments{
\item{obj}{A data frame containing time point text descriptions.}

\item{domain}{The domain code as character (default: "PC" for pharmacokinetic).
Used to determine the column name containing time point descriptions.}
}
\value{
A data frame with a column representing the unique values of the
xxTPT variable and a NTIME column with the time in hours.
}
\description{
Extracts numeric time values (in hours) from time point text descriptions.
This function handles various formats including pre-dose notations, hour and
minute specifications, time ranges, and day information.
}
\details{
The function recognizes and processes the following patterns:
\itemize{
\item Pre-dose notations (e.g., "PRE-DOSE", "PREDOSE") are converted to 0
\item Hour specifications (e.g., "1H POST-DOSE", "2.5 HOURS POST DOSE")
\item Minute specifications (e.g., "30 MIN POST DOSE") are converted to hours
\item Time ranges (e.g., "0.5H TO 2H POST-DOSE") - the later time is used
\item Day information (e.g., "DAY1 - 2 HOURS POST ADMINISTRATION")
}
}
\examples{
df <- data.frame(PCTPT = c("PRE-DOSE", "1H POST-DOSE", "2.5 HRS POST-DOSE"))
make_ntime_from_tpt(df)  # Returns c(0, 1, 2.5)

}
