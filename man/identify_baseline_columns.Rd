% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{identify_baseline_columns}
\alias{identify_baseline_columns}
\title{Identify baseline columns in a data frame}
\usage{
identify_baseline_columns(df, id_col = "ID")
}
\arguments{
\item{df}{A data frame.}

\item{id_col}{Character string specifying the ID column name. Defaults to "ID".}
}
\value{
A character vector of column names that are baseline columns (constant
per ID). Returns an empty character vector if no baseline columns are found.
}
\description{
Identifies columns that are constant (baseline) for each ID value. A baseline
column is one where all rows with the same ID have the same value.
}
\examples{
# Create example data frame
df <- tibble::tribble(
  ~ID, ~SEX, ~AGE, ~TIME, ~DV,
  1,   "M",  25,   0,     10,
  1,   "M",  25,   1,     12,
  1,   "M",  25,   2,     15,
  2,   "F",  30,   0,     8,
  2,   "F",  30,   1,     9,
  2,   "F",  30,   2,     11
)
identify_baseline_columns(df, id_col = "ID")
# Returns: c("SEX", "AGE")
}
